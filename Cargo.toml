[workspace]
members = [
    "node",
    "pallets/template",
	"pallets/slashing-voting",
    "runtime",
]
[profile.release]
panic = "unwind"

[profile.fast]
inherits = "release"
opt-level = 0
lto = "off"
incremental = true
codegen-units = 256

